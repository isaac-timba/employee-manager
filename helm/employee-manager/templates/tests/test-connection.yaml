apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "employee-manager.fullname" . }}-test-connection"
  labels:
    {{- include "employee-manager.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "employee-manager.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
