spring:
  application:
    name:employee-manager
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:employeedb
    username: sa
    password: ""
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  h2:
    console:
      enabled: true
      path: /h2-console

server:
  port: 8080
